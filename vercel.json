{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd shared && npm install && npm run build && cd ../client && npm ci && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
